# Specguard output validation

Validates the output styles are tested

## Scenarios are grouped, indentation is 2 spaces

In verbose mode, we display the steps grouped by their parent scenario.

The scenarios are indented 2 spaces. The steps are indented 4 spaces.

levels: e2e

```specguard
Run specguard against the indentation fixture
Each scenario line is indented 2 spaces
Each step line is indented 4 spaces
Steps are grouped by scenario
```
